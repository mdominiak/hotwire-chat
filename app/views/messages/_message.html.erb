<div class='p-3'>
  <div>
    <span class='fw-bold'><%= h message.author.name %></span>&nbsp;
    <span class='fw-light fs-7'><%= h message.created_at.to_s(:short) %></span>
  </div>
  <div><%= h message.content%></div>
</div>
